using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio3 : MonoBehaviour
{
    //Variables que almacenan los caracteres
    private string DataA = "Teenage Mutant";
    private string DataB = "Ninja Turtles";
    // Start is called before the first frame update
    void Start()
    {
        //Los caracteres se concatenan y se muestran en la consola
        Debug.Log(DataA + " " + DataB);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
